// Stub for @wordpress/i18n — not installed as a standalone npm package.
// Provides the translation functions used in the source components.
module.exports = {
	__: ( str ) => str,
	_x: ( str ) => str,
	_n: ( singular ) => singular,
	_nx: ( singular ) => singular,
	sprintf: ( fmt ) => fmt,
};
